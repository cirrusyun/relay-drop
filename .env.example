# 复制为 .env 后再填写。不要把真实密码或哈希提交到版本库。
DOMAIN=drop.example.com
AUTH_USER=replace-with-a-non-obvious-username
# Argon2id 哈希包含多个 $ 字符，必须使用单引号避免 Compose 变量插值。
AUTH_PASSWORD_HASH='replace-with-an-argon2id-hash'

# 单个文件 4 GiB；整个共享空间严格限制为 20 GiB。
MAX_UPLOAD_BYTES=4294967296
MAX_STORAGE_BYTES=21474836480
